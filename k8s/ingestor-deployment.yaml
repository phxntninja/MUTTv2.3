apiVersion: apps/v1
kind: Deployment
metadata:
  name: mutt-ingestor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mutt-ingestor
  template:
    metadata:
      labels:
        app: mutt-ingestor
    spec:
      containers:
        - name: ingestor
          image: mutt-ingestor:2.5
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: mutt-config
            - secretRef:
                name: mutt-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: mutt-ingestor
spec:
  selector:
    app: mutt-ingestor
  ports:
    - name: http
      port: 8080
      targetPort: 8080
